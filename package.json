{
  "name": "finalassignment2",
  "version": "1.0.0",
  "description": "- schema - install package      - npm i express pg sequelize cors     - npm init -y - install sequelize     - npm install --save-dev sequelize-cli  -> to install sequelize-cli     - npx sequelize-cli init  -> to init sequelize-cli command & generate 4 dir     - pada bagian config sesuaikan DB postgres - create database     - npx sequelize db:create",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.12.0",
    "sequelize": "^6.37.3"
  },
  "devDependencies": {
    "sequelize-cli": "^6.6.2"
  }
}
